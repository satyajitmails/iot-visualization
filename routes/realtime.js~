var express = require('express');
var router = express.Router();

/* GET historian page. */
router.get('/', function(req, res) {
  res.render('historian', { title: 'Historical Data' });
});

module.exports = router;
